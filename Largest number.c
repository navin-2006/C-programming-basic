#include <stdio.h>

int main() {
    int a,b;
    
    scanf("%d",&a);
    scanf("%d",&b);
    if(a>b)
   {
       printf("a is largest");
       
   }
   else 
      printf("b is largest");

    return 0;
}
